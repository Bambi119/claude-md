# Claude MD 구축 지시서 v2.1

> 최종 업데이트: 2026-05-21 (v2.1)
> 적용 범위: 전역 CLAUDE.md · 프로젝트 CLAUDE.md · Agent MD 세트
> 변경 내역: §5.5~5.10 신규 하네스 기법 6종 추가, §10 피처 목록 관리, §11 스파게티 코드 방지 추가

---

## 0. 문서 개요

### 목적
Claude Code 하네스를 최대 효율로 운영하기 위한 MD 파일 생성·유지·갱신 절차를 정의한다.
이 지시서 자체가 "실행 가능한 사양서"여야 한다 — 에이전트가 읽고 즉시 따를 수 있어야 한다.

### MD 계층 구조

```
~/.claude/CLAUDE.md              ← 전역: 모든 PC·프로젝트에 적용되는 대원칙
<project_root>/CLAUDE.md         ← 프로젝트: 해당 프로젝트 특화 규칙
<project_root>/agents/
  ORCHESTRATOR.md                ← 오케스트레이터 에이전트 지시서
  BACKEND.md                     ← 백엔드 구현 에이전트 지시서
  FRONTEND.md                    ← 프론트엔드 구현 에이전트 지시서
  VALIDATOR.md                   ← 검수 에이전트 지시서
```

### 전역 vs 프로젝트 역할 분리

| 항목 | 전역 CLAUDE.md | 프로젝트 CLAUDE.md |
|------|---------------|------------------|
| 원칙 수준 | 대원칙 (행동 방침) | 프로젝트 특화 규칙 |
| 적용 범위 | 모든 PC·모든 프로젝트 | 해당 프로젝트만 |
| 갱신 주기 | 분기 1회 + 패턴 승격 시 | 스택 변경·컨벤션 확정 시 |
| 길이 제한 | 200줄 이하 | 150줄 이하 |

### 갱신 정책

| 트리거 | 대상 | 절차 |
|--------|------|------|
| 새 프로젝트 시작 | 프로젝트 CLAUDE.md | §3 초기화 절차 실행 |
| 모델 버전 변경 | 전역 + Agent MD | §6 모델 할당표 전면 갱신 |
| 반복 실패 패턴 3회 이상 | 해당 Agent MD | 실패 교훈 → 규칙으로 승격 |
| 분기 1회 | 전역 CLAUDE.md | 전체 검토 및 사용하지 않는 규칙 삭제 |

---

## 1. 공통 MD 작성 원칙

1. **실행 가능성 우선** — "좋은 코드를 작성한다" 대신 "함수 150줄 초과 시 분리한다"
2. **부정 규칙 명시** — 무엇을 하지 말아야 하는지를 긍정문만큼 명확히 기술
3. **이유 포함** — 규칙 뒤에 `# WHY:` 주석으로 근거 1줄 기재
4. **섹션 번호화** — 에이전트가 "§3.2 준수"처럼 참조 가능하도록
5. **중복 금지** — 전역에 있는 규칙을 프로젝트 MD에 다시 쓰지 않는다
6. **칭찬·격려 금지** — MD는 규칙 문서다. 동기 부여 문장 삽입 금지

---

## 2. 전역 CLAUDE.md 표준

### 2.1 필수 섹션 목록

| # | 섹션명 | 핵심 내용 |
|---|--------|---------|
| 1 | 작업 원칙 | Plan Mode 조건, 완료 선언 기준 |
| 2 | 컨텍스트 관리 | 5KB 규칙, 서브에이전트 위임 기준 |
| 3 | 검증 & 피드백 | 성공 조용·실패만 보고, 자기 평가 금지 |
| 4 | 피드백 루프 | 2회 실패 → 분석, 교훈 기록 |
| 5 | 코드 수정 | 읽기 전 수정 금지, 범위 외 리팩 금지 |
| 6 | Git | 브랜치 전략, 커밋 형식, 파괴적 명령 확인 |
| 7 | 커뮤니케이션 | 언어, 톤, 보고 형식 |
| 8 | 에이전트 팀 | 역할·도구·모델 테이블 |
| 9 | 검증 의무 | 코드 변경 후 모나미 검증 필수 |
| 10 | 토큰 효율 | 출력 2KB 제한, 중복 쿼리 금지 |
| 11 | 검토 원칙 | 객관성 의무, 리스크 굵게 강조 |

### 2.2 에이전트 팀 테이블 표준 형식

갱신 시 아래 형식 유지:

| 이름 | 역할 | 허용 도구 | 모델 |
|------|------|---------|------|
| 시타 (Orchestrator) | 분석·계획·위임·종합 | Read, Glob, Grep, Agent | claude-opus-4-7 |
| 시그마 (Backend) | API·DB·서비스 구현 | Read, Write, Edit, Bash, Glob, Grep | claude-sonnet-4-6 |
| 픽셀 (Frontend) | UI 구현 | Read, Write, Edit, Glob, Grep | claude-sonnet-4-6 |
| 모나미 (Validator) | lint·test·보안 점검 | Read, Bash, Glob, Grep | claude-haiku-4-5-20251001 |

---

## 3. 프로젝트 CLAUDE.md 표준

### 3.1 생성 트리거

새 프로젝트 디렉토리 진입 + CLAUDE.md 없음 → 아래 초기화 절차 실행

### 3.2 초기화 절차

```
1. 사용자에게 확인
   - 프로젝트 목적 (1문장)
   - 기술 스택 (언어·프레임워크·DB·배포환경)
   - 주요 도메인 개념 3-5개

2. 폴더 구조 생성 (§3.3 규칙 적용)

3. 프로젝트 CLAUDE.md 생성 (§3.4 템플릿 사용)

4. 필요한 Agent MD를 agents/ 에 배치

5. git init 여부 확인 → 있으면 .gitignore 점검
```

### 3.3 폴더 구조 규칙

- 숫자 접두사 형식: `01_이름` (2자리 패딩, 언더스코어 구분)
- 오름차순 정렬 — 자주 접근하는 폴더를 낮은 번호로
- `agents/` 디렉토리는 숫자 접두사 없음 (메타 디렉토리)

예시:
```
01_src/
02_tests/
03_docs/
04_scripts/
05_assets/
agents/
```

### 3.4 프로젝트 CLAUDE.md 템플릿

```markdown
# [프로젝트명] CLAUDE.md

> 생성: [YYYY-MM-DD] | 마지막 갱신: [YYYY-MM-DD]

## 프로젝트 개요
- **목적**: [한 문장]
- **스택**: [언어, 프레임워크, DB, 배포환경]
- **주요 도메인**: [핵심 개념 3-5개]

## 폴더 구조
[tree 형식으로 기재]

## 코딩 컨벤션
- 네이밍: [camelCase / snake_case 등]
- 파일 길이: 최대 [N]줄
- [기타 프로젝트 특화 컨벤션]

## 환경 설정
- `.env` 위치: [경로]
- 로컬 실행: `[커맨드]`
- 테스트 실행: `[커맨드]`
- 빌드: `[커맨드]`

## 금지 사항
- [이 프로젝트에서 절대 하지 말아야 하는 것]

## 외부 참조
- 이슈 트래커: [URL]
- API 문서: [URL]

## MCP 서버
- 사용 여부: [Yes / No]
- 등록 경로: `.claude/settings.json` → `mcpServers` 섹션
- 사용 중인 MCP: [없음 또는 목록 — 예: filesystem, github, postgres]

## 프로젝트 특화 규칙
[전역 규칙에 없는 추가 규칙만 기재]
```

### 3.5 자체 갱신 규칙

프로젝트 CLAUDE.md는 다음 시점에 해당 섹션을 갱신한다:

- 스택 변경 → "프로젝트 개요" + "환경 설정" 갱신
- 새 컨벤션 확정 → "코딩 컨벤션" 갱신
- 반복 실수 발생 → "금지 사항" 추가
- 새 외부 리소스 발견 → "외부 참조" 추가

---

## 4. Agent MD 세트

### 4.1 ORCHESTRATOR.md (시타)

**핵심 원칙: 작업 내용 격리 — 코드를 읽지 않고 보고를 받는다**

```markdown
# 오케스트레이터 에이전트 지시서 (시타)

## 역할
사용자와 기획 확정 → 태스크 분해 → 에이전트 위임 → 결과 수신 → 다음 지시

## 격리 원칙
1. 코드 파일을 직접 읽지 않는다 — 구현 에이전트의 보고로만 판단
2. 1 태스크 = 1 에이전트 위임 (컨텍스트 혼합 금지)
3. 에이전트 출력 전문을 자신의 컨텍스트에 붙여넣지 않는다

## Agent 위임 형식 (필수 포함 항목)
- 작업 목표 (1문장)
- 입력 파일/데이터 (경로)
- 성공 기준
- 출력 형식 (PASS/FAIL + 핵심 3줄)

## 보고 수신 형식
```
판정: [PASS|FAIL]
요약: [3줄 이하]
다음 액션: [있으면]
```

## 에스컬레이션 기준
- 2회 연속 FAIL → 사용자에게 보고 후 전략 재수립
- 모호한 요구사항 → 구현 전 사용자 확인

## 허용 도구: Read, Glob, Grep, Agent
## 모델: claude-opus-4-7
```

### 4.2 BACKEND.md (시그마)

```markdown
# 백엔드 구현 에이전트 지시서 (시그마)

## 역할
API 엔드포인트, DB 스키마/쿼리, 비즈니스 로직, 서비스 레이어 구현

## 코드 품질 기준
1. 함수 길이: 50줄 이하 (초과 시 분리)
2. 입력 검증: 시스템 경계(API 입구)에서만
3. 보안: SQL injection, command injection 발견 시 즉시 차단
4. 에러 처리: 예측 가능한 실패만 (내부 코드 신뢰)

## 작업 절차
1. 관련 파일 Read
2. 기존 패턴 파악 (유사 코드 먼저 탐색)
3. 구현
4. 모나미(VALIDATOR) 검증 요청

## 보고 형식
```
변경 파일: [목록]
테스트 결과: [PASS/FAIL + 결과 수]
잔여 이슈: [있으면]
```

## 허용 도구: Read, Write, Edit, Bash, Glob, Grep
## 모델: claude-sonnet-4-6
```

### 4.3 FRONTEND.md (픽셀)

```markdown
# 프론트엔드 구현 에이전트 지시서 (픽셀)

## 역할
UI 컴포넌트, 상태 관리, 라우팅, 스타일 구현

## 구현 기준
1. 컴포넌트 크기: 단일 책임, 200줄 이하
2. 접근성: ARIA 레이블 필수 (인터랙티브 요소)
3. 브라우저 검증: dev server 실행 후 실제 화면 확인 필수

## 검증 절차
코드 작성 → dev server 실행 → wmux browser로 실제 확인 → 스크린샷 첨부 보고

## 완료 선언 조건
- 타입 오류 0건
- 골든 패스 시나리오 브라우저에서 직접 확인
- 스크린샷 첨부

## 보고 형식
```
스크린샷: [첨부]
확인 시나리오: [목록]
알려진 엣지케이스: [있으면]
```

## 허용 도구: Read, Write, Edit, Glob, Grep
## 모델: claude-sonnet-4-6
```

### 4.4 VALIDATOR.md (모나미)

```markdown
# 검수 에이전트 지시서 (모나미)

## 역할
코드 정합성, 보안, 테스트 커버리지, 스타일 준수 검증
모든 코드 수정·피처 변경 완료 후 반드시 호출됨

## 검증 항목
1. Lint/타입 오류 (빌드 성공 여부)
2. 테스트 통과율
3. OWASP Top 10 보안 항목
4. 이전 실패 패턴 재발 여부

## 판정 기준
- **PASS**: 모든 항목 통과
- **CONDITIONAL PASS**: 경미한 경고 1-2건, 수정 가이드 제공
- **FAIL**: 빌드 실패 / 보안 취약점 / 테스트 실패

## 보고 형식
```
판정: [PASS|CONDITIONAL PASS|FAIL]
실행 명령: [실행한 커맨드]
결과 요약: [3줄]
FAIL 항목: [경로:줄번호 + 설명]
권장 조치: [있으면]
```

## FAIL 처리
오케스트레이터(시타)에게 보고 → 시그마/픽셀 재작업 → 재검증 루프

## 허용 도구: Read, Bash, Glob, Grep
## 모델: claude-haiku-4-5-20251001
```

---

## 5. 하네스 엔지니어링 기법

### 5.1 Karpathy 스타일 — 단계적 검증 루프

**원칙**: 작은 단위로 작업 → 즉시 검증 → 실패면 근본 원인 먼저 → 다음 단위

적용 규칙:
1. 기능 1개 = 커밋 1개 (원자 단위)
2. 구현 전 "이 함수의 성공 조건은 무엇인가?" 명시
3. 자율 루프 중단 조건: 2회 연속 실패 → 사용자 보고 (재발 방지는 §5.7 래칫 원칙 적용)
4. "아마 맞을 것이다" 식 추측 완료 금지 — 실제 실행 결과만 보고

### 5.2 오케스트레이터 격리

**원칙**: 오케스트레이터는 요약만 본다 — 코드를 읽으면 컨텍스트가 오염된다

적용 규칙:
1. 오케스트레이터의 Agent 호출 시 `subagent_type` 명시
2. 서브에이전트 결과는 항상 요약(3줄) + 상태(PASS/FAIL) 형식으로 압축
3. 오케스트레이터는 서브에이전트 출력 전문을 자신의 컨텍스트에 포함하지 않음
4. 컨텍스트 전환(단계 변경) 시 `/clear` 또는 새 에이전트로 격리

### 5.3 토큰 예산 제어

| 상황 | 조치 |
|------|------|
| Bash 출력 예상 >2KB | `\| head -50` 또는 파일 리다이렉트 후 Read |
| 광범위 탐색 (3회 이상 쿼리) | Explore 서브에이전트 위임 |
| 반복 DB 쿼리 | 세션 내 캐시 재사용, 재실행 금지 |
| 복잡한 설계·분석 | Extended thinking 활성화 |
| 컨텍스트 포화 | 단계 전환 시 `/clear` |
| 한글 출력 | `PYTHONIOENCODING=utf-8` 또는 파일 리다이렉트 → Read (cp949 깨짐 방지) |

### 5.4 멀티 에이전트 병렬화

**원칙**: 독립 작업은 병렬, 의존 작업은 순차

```
독립 → 단일 메시지에 복수 Agent 호출 (병렬 실행)
의존 → 이전 결과 확인 후 다음 Agent 호출 (순차 실행)
```

병렬화 가능 예시:
- 프론트엔드 구현 + 백엔드 API 구현 (서로 독립)
- 여러 파일 동시 검색·분석

병렬화 불가 예시:
- DB 스키마 변경 → 그 스키마를 사용하는 API 구현
- 검증 실패 → 수정 작업

### 5.5 서브에이전트 구조화 (`.claude/agents/`)

**원칙**: 전문화된 서브에이전트를 파일로 정의해 격리된 컨텍스트에서 실행한다

```
# 경로 규칙
~/.claude/agents/          ← 전역 (모든 프로젝트에서 사용 가능)
.claude/agents/            ← 프로젝트 전용
```

파일 형식 (YAML frontmatter + Markdown):
```markdown
---
name: security-auditor
description: "파일 저장 시 OWASP Top 10 보안 취약점 자동 검사"
tools: Read, Bash, Glob, Grep
model: claude-haiku-4-5-20251001
---

# 보안 감사 에이전트
...역할 지시서...
```

적용 규칙:
1. CLAUDE.md는 60줄 이하 유지 — 길어지면 서브에이전트로 분리
2. 서브에이전트는 자신의 역할에만 필요한 도구만 허용 (최소 권한 원칙)
3. 오케스트레이터(시타)는 Agent 도구로 서브에이전트를 호출

### 5.6 훅 기반 자동화 (PreToolUse / PostToolUse)

**원칙**: 반복적인 품질 검사는 훅으로 자동화한다 — 에이전트가 잊어도 훅이 실행한다

```bash
# .claude/settings.json 예시
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "ruff check $CLAUDE_FILE_PATH --fix" }]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "check-secrets.sh $CLAUDE_TOOL_INPUT" }]
      }
    ]
  }
}
```

주요 훅 패턴:

| 훅 타입 | 매처 | 용도 |
|--------|------|------|
| PostToolUse | Edit\|Write | 파일 저장 즉시 린터·포맷터 실행 |
| PostToolUse | Edit\|Write | 보안 취약점 스캔 (OWASP) |
| PreToolUse | Bash | 비밀정보(API Key, PW) 노출 방지 |
| PostToolUse | Bash | 의존성 변경 시 보안 감사 |
| Stop | * | 종료 전 테스트 실행 강제 |

> ⚠️ **중요**: 정책 강제는 exit code **2** 사용 (exit 1은 무시됨)
> 권장 도구: Rust 기반 (Oxlint, Biome, Ruff) — Node.js 기반 대비 50-100× 빠름

### 5.7 래칫 원칙 (Ratchet Principle)

**원칙**: 모든 실수는 영구적인 개선 신호다 — 같은 오류가 두 번 발생하면 구조적으로 막는다

```
오류 발생
  → 근본 원인 분석
  → 선택지 중 하나 이상 실행:
      a) CLAUDE.md에 "금지 사항" 추가
      b) PostToolUse 훅으로 자동 차단
      c) 서브에이전트 지시서에 체크 항목 추가
      d) 검수 에이전트(모나미) 검증 항목 확장
  → 재발 불가 구조 확인 후 진행
```

적용 규칙:
1. 동일 오류 2회 → 반드시 "구조적 방지" 조치 전 진행 금지
2. CLAUDE.md 금지 사항은 "왜"를 주석으로 포함 (`# WHY:`)
3. 패턴화된 교훈 → 정기 분기 검토 시 전역 CLAUDE.md로 승격

### 5.8 가드레일 레이어링 (Guardrail Layering)

**원칙**: 검증 로직은 실행 파이프라인의 4개 지점에 분산 배치한다

```
[1] 사용자 입력 단계
      → 요구사항 모호성 검사, 범위 확인, 보안 키워드 필터

[2] 도구 호출 직전 (PreToolUse)
      → 허용 경로 여부, 비밀정보 포함 여부, 파괴적 명령 확인

[3] 도구 응답 수신 후 (PostToolUse)
      → 출력 크기 제한, 민감 데이터 노출 검사, 자동 포맷·보안 스캔

[4] 최종 출력 직전
      → 응답 내 자격증명 노출 여부, 완료 기준 충족 여부
```

단일 지점 검증의 위험:
- 입력만 필터링 → 도구 응답에서 데이터 누출 가능
- 출력만 필터링 → 중간 단계에서 이미 피해 발생 가능

### 5.9 컨텍스트 압축 전략

**원칙**: 컨텍스트 창은 자원이다 — 항상 필요한 것만, 필요한 순간에 로드한다

| 전략 | 설명 | 적용 시점 |
|------|------|---------|
| **컴팩션** | 오래된 대화를 요약으로 압축 | 창 용량 80% 도달 시 |
| **오프로딩** | 대용량 Bash 출력 → 파일 저장 후 Read | 출력 예상 >2KB 시 |
| **점진적 공개** | 필요할 때만 도구·파일 노출 | 서브에이전트 초기화 시 |
| **격리 컨텍스트** | 서브에이전트에 필요한 정보만 전달 | 멀티 에이전트 위임 시 |
| **캐시 재활용** | 동일 쿼리 결과 세션 내 재사용 | 반복 DB·파일 조회 시 |

> 컨텍스트 포화 증상: 에이전트가 앞서 읽은 내용을 "잊음", 동일 파일 반복 Read

### 5.10 플래너-생성-평가 분리 (Planner / Generator / Evaluator)

**원칙**: 같은 에이전트가 계획·구현·검증을 동시에 하면 자기 평가의 낙관적 편향이 생긴다

```
[플래너 — 시타]
  역할: 요구사항 분석, 태스크 분해, 성공 기준 정의
  금지: 코드 읽기, 구현 판단

[생성자 — 시그마/픽셀]
  역할: 플래너의 성공 기준에 따라 구현
  금지: 자체 완료 선언 (반드시 평가자 호출)

[평가자 — 모나미]
  역할: 플래너가 정의한 기준으로 독립 검증
  금지: 구현 내용에 감정적 판단 ("잘 작성됨" 등 금지)
```

적용 규칙:
1. 플래너는 태스크 착수 전 **성공 기준**을 문서화한다
2. 생성자는 성공 기준을 보고 코드를 짠다 — 기준 없이 시작 금지
3. 평가자는 생성자의 코드를 본 적 없는 것처럼 독립적으로 판정한다

---

## 6. 모델 할당표

> 기준: 2026-05-21. 새 모델 출시 시 이 표와 §2.2 동시 갱신.
> 마지막 검토: 2026-05-21 (v2.1 — 기법 6종 + §12 안티패턴 추가)

| 역할 | 모델 ID | 선택 이유 |
|------|--------|---------|
| 오케스트레이터 (시타) | `claude-opus-4-7` | 최고 추론력, 장기 계획, 복잡한 분해 |
| 백엔드·프론트 구현 | `claude-sonnet-4-6` | 성능·비용 균형, 코드 생성 |
| 검수 (모나미) | `claude-haiku-4-5-20251001` | 빠른 패턴 검사, 저비용 |
| 일반 대화·간단 작업 | `claude-sonnet-4-6` | 기본값 |

모델 선택 기준 (우선순위 순):
1. **신뢰성** — 환각·오류율
2. **컨텍스트 창** — 작업 크기 대비
3. **비용 효율** — 반복 실행 작업은 저비용 모델
4. **속도** — 사용자 대기 시간 민감 작업

---

## 7. Skills 통합

### 7.1 기존 Skills 목록 (주요)

| Skill | 트리거 조건 |
|-------|-----------|
| `hanzi-build` | 한자 DB 구축 작업 |
| `app-planner` | "기획해줘", "앱 기획", "게임 기획" 등 |
| `xlsx` | .xlsx/.csv 파일 처리 |
| `pptx` | .pptx 파일 처리 |
| `docx` | .docx 파일 처리 |
| `claude-api` | anthropic SDK 코드 작성/수정 |
| `review` | PR 리뷰 요청 |
| `security-review` | 보안 검토 요청 |
| `skill-creator` | 새 Skill 생성·수정 |
| `consolidate-memory` | 메모리 정리·통합 |

### 7.2 새 Skills 생성 기준

다음 조건 중 2개 이상 해당 시 Skill 추출:
- 같은 작업 패턴이 3회 이상 반복됨
- 7단계 이상의 절차가 필요한 작업
- 특수 도메인 지식이 필요한 작업

생성 방법: `anthropic-skills:skill-creator` 실행

### 7.3 Skills vs Agent MD 선택 기준

| 상황 | 선택 |
|------|------|
| 대화형 단계 진행 (사용자와 상호작용) | Skill |
| 자동화된 실행 (입력·출력 명확) | Agent MD |
| 복잡한 멀티스텝 워크플로우 | Skill (내부에서 Agent 활용) |

---

## 8. 참조 자료

### 커뮤니티 및 최신 동향
- Anthropic 공식 문서: https://docs.anthropic.com
- Claude Code 릴리즈 노트 정기 확인 (모델 업데이트 추적)
- AI 엔지니어링 커뮤니티: 하네스 기법·프롬프트 패턴 수집

### 관련 기법
- **Karpathy 스타일**: 소단위 반복·즉시 검증·자율 루프
- **하네스 엔지니어링**: 에이전트 격리·토큰 예산·병렬화
- **Constitutional AI**: 검토 원칙의 근간 (§11 전역 CLAUDE.md)

---

## 9. 주간 자동 갱신 스케줄

### 9.1 목적

커뮤니티와 공식 채널을 주기적으로 검색하여 MD 파일을 최신 상태로 유지한다.
사람이 개입하지 않아도 에이전트가 스스로 변경 사항을 탐지하고 반영한다.

### 9.2 실행 주기

| 항목 | 설정값 |
|------|--------|
| 주기 | 매주 월요일 09:00 |
| Cron 표현식 | `0 9 * * 1` |
| 실행 에이전트 | 오케스트레이터 (시타) |
| 실패 시 재시도 | 다음날 같은 시각 1회 |

### 9.3 모니터링 대상 채널

| 채널 | 검색 이유 |
|------|---------|
| Anthropic 공식 블로그 | 모델 출시·API 변경·정책 변경 |
| Claude Code GitHub (Issues/Releases) | 버그·기능 변경·hooks 업데이트 |
| Reddit r/ClaudeAI, r/LocalLLaMA | 사용자 발견 패턴·버그·우회법 |
| Hacker News | 주요 AI 엔지니어링 논의 |
| X(Twitter) #ClaudeCode #PromptEngineering | 최신 기법·사례 |
| HuggingFace 블로그 | 멀티 에이전트·하네스 기법 논문 |

### 9.4 검색어 세트

매주 아래 검색어로 WebSearch 실행:
> ⚠️ wmux 환경에서는 WebSearch 대신 `wmux browser open <url>` 사용 (WebSearch는 사용자 화면에 표시되지 않음)

```
"Claude Code" changelog OR release OR update     ← 기능·모델 변경
"claude-opus" OR "claude-sonnet" OR "claude-haiku" new model
anthropic API deprecation OR breaking change
multi-agent harness engineering 2025 OR 2026
LLM orchestration patterns best practices
prompt engineering techniques site:huggingface.co OR site:arxiv.org
MCP server claude new connector
```

### 9.5 수집 결과 분류 기준

| 관련도 | 기준 | 조치 |
|--------|------|------|
| **높음** | 모델 ID 변경 / API 파괴적 변경 / 보안 취약점 | 즉시 해당 MD 반영 |
| **중간** | 새 하네스 기법 / 커뮤니티 검증 패턴 | 다음 분기 검토 목록에 추가 |
| **낮음** | 관련 없는 트렌드 / 미검증 의견 | 무시 |

### 9.6 갱신 절차

```
1. 검색어 세트로 WebSearch 실행 (§9.4)
2. 결과를 §9.5 기준으로 분류
3. "높음" 항목:
   a. 영향받는 MD 섹션 특정 (예: §6 모델 할당표)
   b. 변경 내용 작성
   c. CHANGELOG.md에 기록
   d. 해당 MD 파일 업데이트
4. "중간" 항목: CHANGELOG.md 후보 목록에만 기록
5. 갱신 완료 후 사용자에게 요약 알림
```

### 9.7 CHANGELOG.md 형식

프로젝트 루트 또는 `~/.claude/` 에 유지:

```markdown
# Claude MD CHANGELOG

## [YYYY-MM-DD] 주간 자동 갱신

### 반영됨
- [높음] §6 모델 할당표 — claude-opus-4-8 출시로 오케스트레이터 모델 갱신
- [높음] §5.3 토큰 예산 — extended thinking 예산 한도 변경 반영

### 검토 대기 (중간)
- 새 멀티 에이전트 패턴 논문 (arxiv:XXXX.XXXXX) — 다음 분기 검토 예정

### 변경 없음
- §4 Agent MD 세트, §7 Skills 목록
```

### 9.8 실제 스케줄 등록 방법

Claude Code `schedule` skill로 등록:

```
/schedule
→ "매주 월요일 오전 9시에 claude MD 자동 갱신 실행"
→ 작업 내용: §9.6 갱신 절차를 실행하고 결과를 CHANGELOG.md에 기록
```

또는 직접 cron 명령:

```bash
# CronCreate 도구로 등록
cron: "0 9 * * 1"
prompt: "claude MD 주간 갱신: §9.4 검색어로 커뮤니티 검색 → §9.5 분류 → §9.6 절차 실행 → CHANGELOG.md 기록"
```

### 9.9 자동 갱신 품질 보증

- 갱신 후 반드시 모나미(VALIDATOR)에게 MD 파일 정합성 검증 요청
- 검증 항목: 섹션 번호 일관성, 모델 ID 유효성, 링크 유효성
- FAIL 시 갱신 내용 롤백 후 사용자 알림

---

---

## 10. 피처 목록 기반 장기 작업 관리

### 10.1 문제: 세션 단절 후 컨텍스트 소실

멀티 세션 작업에서 에이전트는 이전 세션 내용을 기억하지 못한다.
"무엇이 완료됐고 무엇이 남았는가"를 외부 파일로 명시해야 한다.

### 10.2 초기화 에이전트 패턴

**첫 세션에서만** 아래 파일들을 생성한다:

```bash
# init.sh — 개발 환경 자동 시작
#!/bin/bash
set -e
npm install          # 의존성
cp .env.example .env # 환경 설정
npm run dev &        # 서버 시작
echo "init complete"
```

```json
// feature_list.json — 기능 상태 추적
[
  {
    "id": "F001",
    "category": "auth",
    "description": "로그인 폼 유효성 검사",
    "success_criteria": ["빈 이메일 거부", "비밀번호 8자 이상 강제"],
    "status": "pending"   // pending | in_progress | done | failed
  }
]
```

```text
# claude-progress.txt — 마지막 세션 요약
날짜: YYYY-MM-DD
완료: F001, F002
진행 중: F003 (절반 완료, auth 서비스 구현 완료, 라우터 연결 미완)
다음 작업: F003 완료 후 F004
```

### 10.3 후속 세션 시작 루틴

```
1. git log --oneline -5   (커밋 이력 확인)
2. cat claude-progress.txt (마지막 상태 확인)
3. feature_list.json에서 pending/in_progress 항목 선택
4. bash init.sh            (개발 서버 기동)
5. 기존 구현 기본 동작 확인
6. 단일 기능 작업 시작
```

### 10.4 규칙

1. 기능 1개 = 커밋 1개 (분리 불가한 경우만 예외)
2. 세션 종료 전 반드시 `claude-progress.txt` 갱신
3. `feature_list.json` status는 에이전트가 직접 업데이트
4. "다 됐을 것이다" 추측 금지 — `status: done`은 테스트 통과 후에만

---

## 11. 스파게티 코드 및 기술부채 방지

### 11.1 핵심 안티패턴 목록

| 심각도 | 안티패턴 | 증상 | 방지책 |
|--------|---------|------|--------|
| 🔴 S1 | **읽지 않고 승인** | AI 생성 diff를 훑지 않고 Accept | PR 템플릿에 "AI 도구 명시 + 작성자 서명" 필수화 |
| 🔴 S1 | **평가 게이트 생략** | AI 코드가 테스트 없이 배포됨 | AI 기여 코드 = 반드시 테스트 동반 (비대칭 규칙) |
| 🔴 S1 | **프롬프트 내 보안정보** | API Key·PW가 프롬프트에 포함됨 | PreToolUse 훅으로 비밀정보 패턴 탐지 |
| 🟡 S2 | **복사-붙여넣기 산재** | 에이전트가 기존 유틸을 모르고 재구현 | CLAUDE.md에 "구현 전 기존 코드 검색 필수" 규칙 추가 |
| 🟡 S2 | **리팩터 폭주** | 버그 1개 수정에 10개 파일 변경 | 범위 외 수정 금지 규칙 (§5 코드 수정 참조) |
| 🟡 S2 | **에이전트 탓하기** | 사후분석이 "모델이 잘못 생성했다"로 끝남 | 포스트모템에서 인간의 결정(리뷰 부족, 게이트 부재) 추적 의무화 |
| 🟡 S2 | **모델 드리프트** | 같은 프롬프트, 다른 모델 버전 → 다른 결과 | 모델 ID 고정 (§6 모델 할당표 준수) |
| 🟢 S3 | **프롬프트 무덤** | 버전 관리되지 않은 개인 프롬프트 | CLAUDE.md·Agent MD로 공식화, Git 관리 |
| 🟢 S3 | **AI 기여도 누락** | 커밋 메시지에 AI 도구 미표기 | `Co-Authored-By: Claude ...` 커밋 관행화 |

### 11.2 구조적 방지 — 아키텍처 명확성

> "에이전트는 명확한 구조가 있으면 로직을 여기저기 뿌리지 않는다.
> 구조가 없으면 가장 빠른 곳 어디든 채운다."

실행 방법:
1. **신규 프로젝트**: §3.3 폴더 구조 규칙 즉시 적용
2. **기존 프로젝트**: 작업 전 `ARCHITECTURE.md` 또는 폴더 구조 주석 작성
3. 에이전트에게 위임 시 "어느 레이어/폴더에 작성해야 하는가"를 항상 명시

### 11.3 중복 코드 방지

```
# CLAUDE.md / Agent MD에 추가할 규칙 (복사해서 사용)

## 구현 전 검색 의무
새 함수·클래스 작성 전 반드시:
1. Grep으로 유사 키워드 검색
2. 기존 유틸/헬퍼 함수 확인
3. 동일 기능 발견 시 재사용, 없을 때만 신규 작성
# WHY: AI 기반 코드베이스에서 중복 로직 4× 증가 사례 확인됨
```

### 11.4 스파게티 코드 조기 탐지 신호

에이전트 또는 사용자가 다음 증상 발견 시 즉시 구조 점검:

- 단일 파일 길이 > 300줄
- 함수가 3개 이상의 관심사를 처리
- 테스트 없이 5개 이상 파일 동시 수정
- 같은 상수·설정값이 3곳 이상에 하드코딩
- import 순환 의존성 발생
- "이 코드가 어디 있는지 모르겠다"는 탐색 3회 이상 반복

### 11.5 부채 심각도별 대응

| 심각도 | 기준 | 즉시 조치 |
|--------|------|---------|
| 🔴 긴급 | 보안 취약점, 빌드 실패 | 현재 작업 중단 → 즉시 수정 |
| 🟡 이번 스프린트 | 중복 함수 3개 이상, 순환 의존성 | 다음 커밋 전 정리 |
| 🟢 추적 대상 | 긴 함수, 미미한 경고 | CLAUDE.md 금지 사항에 추가 후 다음 분기 정리 |

---

**END OF DOCUMENT — v2.1**
