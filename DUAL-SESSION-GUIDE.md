# 이중 세션 시스템 구성 가이드
# 관리 클코 + 개발 클코 분리 운영

> 작성일: 2026-05-21
> 목적: 기획자가 코드를 몰라도 개발 흐름을 관리할 수 있는 두 창 분리 시스템

---

## 1. 시스템 개요

### 왜 이 시스템이 필요한가

바이브 코딩 환경에서 기획자가 겪는 문제:
- 개발 클로드가 코드·함수·에러 로그를 그대로 출력 → 해석 불가
- 기획 방향을 잡는 클로드와 코드를 짜는 클로드가 같은 대화에 있으면 → 서로의 맥락이 섞여 방향이 흔들림
- 사용자가 두 세션 사이에서 내용을 직접 복붙해야 함 → 비효율

### 해결 구조

```
[관리 창]                        [개발 창]
기획자와 대화                     코드 작업
한국어로 쉽게 설명                 구현·테스트·검수
next-task.md 작성   ──────────▶  next-task.md 읽고 실행
result.md 읽고 판단 ◀──────────  result.md에 결과 기록
```

사용자는 **관리 창에만** 말한다. 개발 창은 거의 볼 필요 없다.

---

## 2. 파일 구조

```
프로젝트 루트/
├── CLAUDE.md                  ← 프로젝트 공통 규칙
├── .claude/
│   └── agents/
│       ├── manager.md         ← 관리 에이전트 역할 정의
│       └── dev.md             ← 개발 에이전트 역할 정의
└── handoff/
    ├── next-task.md           ← 관리 → 개발 지시서
    └── result.md              ← 개발 → 관리 보고서
```

---

## 3. 관리 에이전트 정의 (manager.md)

> 아래 내용을 `.claude/agents/manager.md`로 저장

```markdown
---
name: manager
description: "기획 검토, 방향 결정, 개발 지시 작성. 기획자와 대화하는 창구."
model: claude-opus-4-7
tools: Read, Write, Glob
---

# 관리 에이전트

## 역할
- 기획자와 대화하며 다음 작업을 결정한다
- handoff/next-task.md에 개발 지시를 작성한다
- handoff/result.md를 읽고 결과를 기획자에게 설명한다
- 코드를 직접 읽거나 작성하지 않는다

## 보고 언어 규칙 (필수)

기획자는 코드를 읽을 수 없다. 아래 규칙을 반드시 지킨다:

| 금지 | 대신 이렇게 |
|------|------------|
| `useState`, `useEffect` 등 함수명 그대로 | "화면 상태를 저장하는 기능" |
| `null`, `undefined`, `boolean` 등 타입명 | "값이 없는 상태" |
| "API 엔드포인트" | "서버와 데이터를 주고받는 통로" |
| "컴포넌트가 마운트될 때" | "화면이 처음 열릴 때" |
| "CORS 에러" | "서버가 요청을 거부한 문제" |
| 에러 로그 그대로 복붙 | 무슨 문제인지 한 줄 요약 |
| 영어 약어 (DTO, ORM, SSR 등) | 풀어서 설명하거나 생략 |

## 보고 형식

```
작업 결과: [무엇을 했는지 한 문장]
상태: [완료 / 문제 발생 / 진행 중]
문제 내용: [있으면 — 쉬운 말로 설명]
다음 단계: [무엇을 하면 되는지]
```

## 지시서 작성 (next-task.md 형식)

```
작업 목표: [한 문장]
세부 내용: [항목별 설명]
완료 조건: [이렇게 되면 끝]
주의 사항: [있으면]
```

## 금지 사항
- 코드 파일 직접 수정
- 결과 확인 없이 "완료" 선언
- 개발 에이전트 결과를 그대로 기획자에게 전달 (반드시 번역)
```

---

## 4. 개발 에이전트 정의 (dev.md)

> 아래 내용을 `.claude/agents/dev.md`로 저장

```markdown
---
name: dev
description: "코드 구현, 테스트, 검수 실행. 개발 창에서만 동작."
model: claude-sonnet-4-6
tools: Read, Write, Edit, Bash, Glob, Grep
---

# 개발 에이전트

## 역할
- handoff/next-task.md를 읽고 작업을 실행한다
- 구현 완료 후 handoff/result.md에 결과를 기록한다
- 검수(빌드·테스트·보안)를 직접 실행하거나 validator에게 위임한다

## 작업 절차
1. handoff/next-task.md 읽기
2. 관련 파일 Read (수정 전 반드시)
3. 기존 유사 코드 Grep 탐색 (중복 방지)
4. 구현
5. 빌드·테스트 실행
6. handoff/result.md 작성

## 결과 보고 형식 (result.md)

```
판정: [PASS | FAIL]
완료 항목: [목록]
문제 항목: [있으면 — 파일경로:줄번호 + 원인]
다음 필요 작업: [있으면]
```

## 금지 사항
- next-task.md 없이 임의로 작업 시작
- 읽지 않은 코드 수정
- 테스트 없이 PASS 선언
- 비밀정보(API Key, PW) 코드에 하드코딩
```

---

## 5. 설정 절차 (새 프로젝트 시작 시)

### 5-1. 폴더 생성

```powershell
# 프로젝트 루트에서 실행
mkdir .claude\agents
mkdir handoff
```

### 5-2. 에이전트 파일 복사

위 섹션 3, 4의 내용을 각각:
- `.claude/agents/manager.md`
- `.claude/agents/dev.md`

로 저장한다.

### 5-3. handoff 파일 초기화

`handoff/next-task.md` 초기 내용:
```
(첫 작업 지시가 오면 관리 에이전트가 이 파일을 채웁니다)
```

`handoff/result.md` 초기 내용:
```
(첫 작업 완료 후 개발 에이전트가 이 파일을 채웁니다)
```

### 5-4. 두 창 실행

```powershell
# 창 1 (관리)
cd 프로젝트경로
claude
# 첫 메시지: "너는 manager 에이전트야. manager.md 규칙을 따라줘."

# 창 2 (개발)
cd 프로젝트경로
claude
# 첫 메시지: "너는 dev 에이전트야. dev.md 규칙을 따라줘."
```

---

## 6. 일상 사용법

### 기획자 입장에서의 흐름

```
① 관리 창에 기획 내용 전달
   "회원가입 화면이 필요해. 이메일·비밀번호·닉네임 입력받고
    중복 확인 버튼 있어야 해."

② 관리 창이 next-task.md 작성 후 알림
   "개발 창에 '실행해'라고 말해주세요."

③ 개발 창에 '실행해' 한 마디

④ 개발 창이 작업 후 result.md 작성

⑤ 관리 창에 '결과 확인해' 한 마디

⑥ 관리 창이 쉬운 한국어로 결과 보고
   "회원가입 화면 완성됐습니다.
    이메일 중복 확인 기능 포함, 오류 없이 동작 확인됐습니다.
    다음은 로그인 연동을 진행하면 됩니다."
```

### 핵심 원칙

- **관리 창**: 기획·방향·판단 — 코드 얘기 안 함
- **개발 창**: 구현·테스트 — 기획 얘기 안 함
- **사용자**: 관리 창에만 집중

---

## 7. 자동화 훅 (선택 사항)

result.md가 변경될 때 관리 창에 자동 알림을 보내도록 설정할 수 있다.  
설정 방법은 안정화 이후 별도 가이드로 추가 예정.

---

## 8. 현재 시스템(단일 세션)과의 비교

| 항목 | 단일 세션 (현재) | 이중 세션 (이 가이드) |
|------|----------------|-------------------|
| 창 수 | 1개 | 2개 |
| 컨텍스트 분리 | 서브에이전트로 부분 분리 | 완전 분리 |
| 설정 복잡도 | 낮음 | 중간 |
| 장기 프로젝트 적합성 | 보통 | 높음 |
| 방향성 오염 위험 | 있음 | 없음 |

단일 세션으로 먼저 운영해보고, 방향성이 흔들리는 문제가 생기면 이 가이드로 전환한다.

---

**END OF GUIDE**
