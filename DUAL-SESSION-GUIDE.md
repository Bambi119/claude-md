# 이중 세션 시스템 운영 가이드 v2.0
# 관리 세션(시타) + 개발 세션(시그마·픽셀·모나미) 자동화 분리 운영

> 작성일: 2026-05-22
> v1.0 수동 방식에서 v2.0 자동화 방식으로 전면 교체
> 목적: 기획자가 코드를 몰라도 개발 흐름을 관리할 수 있는 자동화 시스템

---

## 1. 시스템 개요

### 핵심 문제와 해결

| 문제 | 해결 |
|------|------|
| 개발 보고가 기술 용어로 가득해 판단 불가 | 모나미가 보고서 작성, 시타가 자연어로 번역 |
| 사용자가 판단을 AI에게 위임 → 프로젝트 표류 | 사용자 결정이 필요한 게이트만 최소화하여 명확히 제시 |
| 개발·관리 컨텍스트 혼합 → 방향 오염 | 세션 물리 분리, 파일 큐로만 통신 |
| 구현자가 자기 작업 평가 → 자기 합리화 | 모나미(검수자)가 사용자 영향 독립 판단 |

### 세션 구조

```
┌──────────────────────────────┬───────────────────────────────────┐
│  관리 세션 (tmux pane 1)      │  개발 세션 (tmux pane 2)           │
│  에이전트: 시타               │  에이전트: 시그마·픽셀·모나미      │
│                              │                                   │
│  기획·관리·소환               │  구현·검수·보고서 작성              │
│  코드 Read 금지               │  기획 판단 금지                    │
│                              │                                   │
│  /loop 로 자기 반복           │  /loop 로 자기 반복               │
│  (5분 주기 큐 확인)           │  (5분 주기 큐 확인)               │
└──────────────┬───────────────┴──────────────┬────────────────────┘
               │           파일 큐             │
               │   01_handoff/queue/           │
               │   ready/      ←── 보고서     │
               │   approvals/  ──→ 결정       │
               └──────────────────────────────┘
```

---

## 2. 파일 큐 구조

```
01_handoff/queue/
├── ready/        ← 개발→관리: 보고서(report) + 관리→개발: 지시서(next-task) 투입
├── processing/   ← 개발 세션이 픽업한 next-task 보관 (중복 픽업 방지)
├── approvals/    ← 관리→개발: 승인/결정 파일
└── done/         ← 처리 완료 보관
```

**기존 01_handoff/ 파일과 구분**

| 파일 | 용도 | 형식 |
|------|------|------|
| `01_handoff/decisions.md` | 세션 간 결정 히스토리 누적 | 텍스트 (end 스킬이 관리) |
| `01_handoff/queue/approvals/` | 실시간 승인/결정 신호 | JSON (자동화 큐) |

---

## 3. 자동화 흐름

```
[개발 세션]                           [관리 세션]
    │                                      │
    │ ① 시그마·픽셀 구현 완료              │
    │ ② 모나미 검수 + report.json 작성     │
    │ ③ ready/report_{ts}_{id}.json 투입  │
    │                          ──감지──→  │ ④ Monitor 자동 감지
    │                                      │ ⑤ 기획서 대조 + 자연어 브리핑
    │                                      │ ⑥ 사용자에게 선택지 제시
    │                                      │ ⑦ approvals/ 에 결정 기록
    │                                      │ ⑧ next-task → ready/ 작성
    │                                      │ ⑨ 보고서 → done/ 이동
    │ ⑩ next-task 자동 픽업               │
    │   (ready→processing, 감시 루프)      │
    │ ⑪ 결정·지시대로 작업 → 다시 ①      │
```

---

## 4. 세션별 시작 절차

같은 프로젝트 폴더에서 Claude Code 창 두 개를 연다.
처음 쓰는 프로젝트면 먼저 `/new`로 폴더 구조와 명령어를 설치한다.

### 관리 세션 (창 1)

```bash
cd 프로젝트경로
claude
```

창이 열리면 입력:
```
/loop 5m /manager
```
→ 시타가 기획서(`plan.md`)를 읽고, **5분마다 `ready/`의 보고서를 자동 확인**한다.

### 개발 세션 (창 2)

```bash
cd 프로젝트경로
claude
```

창이 열리면 입력:
```
/loop 5m /dev
```
→ 개발팀이 **5분마다 `ready/`의 next-task를 자동 확인·처리**한다.
→ 관리 세션이 작업을 발행하면, 다음 주기에 개발 세션이 스스로 픽업한다.

### 자동화의 핵심 — `/loop`

Claude 창은 한 번 잠들면 스스로 깨어나지 못한다.
`/loop`는 **세션이 자기 자신을 주기적으로 깨우게** 하는 Claude Code 기능이다.
각 창에서 위처럼 `/loop 5m /manager`·`/loop 5m /dev`를 한 번씩 걸어 두면,
매니저↔데브가 서로의 작업을 자동으로 잇는다. 사용자는 관리 창에서 승인만 하면 된다.

> `/loop`는 세션 안에서 도는 자체 반복이라 tmux·psmux 등 멀티플렉서와 무관하다.
> 세션 창을 닫으면 멈추고, 루프는 7일 후 만료된다 — 장기 운영 시 다시 건다.
>
> (참고 — 멀티플렉서가 순수 tmux인 환경에서는 외부 감시기 `queue-watcher.sh`도
>  대안으로 쓸 수 있다. 저장소 루트에 보존되어 있다.)

---

## 5. 사용자 입장의 일상 흐름

```
① 관리 세션에 기획 내용 전달
   "회원가입 화면이 필요해. 이메일·비밀번호·닉네임 입력받고
    중복 확인 버튼 있어야 해."

② 시타가 next-task.json 작성 → 개발 세션 자동 실행

③ [개발 세션에서 자동 진행]
   시그마 구현 → 모나미 검수 → report.json 자동 투입

④ 관리 세션에 자동 알림 (Monitor 감지)
   시타가 자연어로 번역:
   "회원가입 화면이 완성됐습니다.
    이메일·비밀번호·닉네임 입력과 중복 확인 버튼이 동작합니다.
    되돌릴 수 없는 변경은 없습니다.

    어떻게 할까요?
    [✅ 승인]  [✏️ 수정 요청]  [🔍 더 알아보기]"

⑤ 사용자: [✅ 승인] 선택

⑥ 시타가 다음 next-task 작성 → 루프
```

**사용자는 관리 세션에만 집중한다. 개발 세션은 볼 필요 없다.**

---

## 6. 에이전트 역할 요약

| 에이전트 | 세션 | 역할 | 보고서 작성 |
|---------|------|------|-----------|
| 시타 | 관리 전담 | 기획·관리·소환·자연어 번역 | 해당 없음 |
| 시그마 | 개발 | 백엔드 구현 | **금지** |
| 픽셀 | 개발 | 프론트 구현 | **금지** |
| 모나미 | 개발 | 검수 + **report.json 작성** | **전담** |

---

## 7. 에스컬레이션 경로

```
개발 세션 판단 불가
    → approvals/ 에 "hold" 결정 파일 작성
    → 관리 세션(시타)이 감지
    → 시타가 사용자에게 상황 설명 요청
    → 사용자 판단 후 새 next-task 발행
```

---

## 8. v1.0 → v2.0 변경 사항

| 항목 | v1.0 (수동) | v2.0 (자동화) |
|------|------------|--------------|
| 파일 구조 | `handoff/next-task.md`, `handoff/result.md` | `01_handoff/queue/` JSON 큐 |
| 보고서 형식 | 텍스트 자유 형식 | 스키마 정의 JSON |
| 관리 감지 | "결과 확인해" 수동 입력 | Monitor 자동 감지 |
| 보고서 작성자 | 개발 에이전트 (자기 합리화 위험) | 모나미 독립 작성 |
| 관리 에이전트 | manager.md 별도 | 시타(ORCHESTRATOR.md)로 통합 |

---

**END OF GUIDE v2.0**
