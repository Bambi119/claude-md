# CLAUDE.md — 전역 하네스 규약 v2.1

> 지시서 기준: `claude MD 구축 지시서 v2.1`
> 마지막 업데이트: 2026-05-21
> 설치: setup.ps1 (Windows) 또는 setup.sh (Mac/Linux) 실행
> ⚠️ wmux 환경에서는 setup 후 wmux가 자동으로 하단 블록을 추가합니다.

---

## 1. 작업 원칙

- 비자명한 작업은 **Plan Mode 먼저** — 파일 읽기 → 계획 → 승인 → 실행
- 한 번에 하나의 기능/변경에 집중. 완료 후 다음으로 넘어간다
- 검증 수단(테스트, 빌드, 스크린샷, diff) 없이 완료 선언 금지
- 추측 완료 금지 — "아마 맞을 것이다" 식 보고 대신 실제 확인 결과만 보고
- 기능 1개 = 커밋 1개 (원자 단위) # WHY: 롤백·검증 단위 명확화

## 2. 컨텍스트 관리

- 무관한 작업 전환 시 `/clear`로 컨텍스트 리셋
- 5KB 초과 파일은 `@`임베드 대신 경로 텍스트로 참조
- 광범위 탐색(3회 이상 쿼리 예상)은 서브에이전트 위임 — 메인 컨텍스트 보호
- 2회 교정 실패 시 `/clear` 후 학습 내용을 반영한 새 프롬프트로 재시작
- 서브에이전트는 `.claude/agents/` 또는 `agents/` 폴더에서 파일로 정의 (지시서 §5.5)
- **새 프로젝트 첫 작업 시** 프로젝트 루트에 `01_handoff/` 폴더가 없으면 자동 생성 # WHY: /end·/start 명령어 정상 동작 보장

## 3. 검증 & 피드백

- **성공은 조용히, 실패만 보고** — 통과 결과로 컨텍스트를 오염시키지 않는다
- 자기 작업을 스스로 평가하지 않는다 — 검증은 도구(테스트·린터·빌드)나 별도 에이전트로
- 훅이 실패하면 원인을 조사한다. `--no-verify` 등 우회 금지

## 4. 피드백 루프 — 검증·학습·재발 방지

- 코드 변경 후 반드시 객관적 검증 실행 (빌드, Playwright, DB 쿼리 등)
- 검증 실패 시 코드가 아닌 **레이아웃/환경부터** 의심 (CSS → 이벤트 → 로직 순)
- 동일 문제 2회 수정 실패 시: 근본 원인 분석 → **래칫 원칙** 적용 (지시서 §5.7)
  - 재발 방지 조치: CLAUDE.md 금지 사항 추가 / 훅 추가 / 검증 항목 확장 중 택1 이상
- 반복된 교훈이 패턴화되면 CLAUDE.md 규칙으로 승격 (분기 1회 검토)
- 사용자 교정("안 된다", "먹통이다")은 즉시 Playwright·스크린샷으로 실제 화면 확인 후 대응

## 5. 코드 수정

- 읽지 않은 코드를 수정하지 않는다
- 요청 범위 밖 리팩토링·개선·주석 추가 금지
- **구현 전 기존 코드 검색 필수** — Grep으로 유사 키워드 확인 후, 없을 때만 신규 작성
  # WHY: AI 기반 코드베이스에서 중복 로직 4× 증가 사례 확인됨
- 보안 취약점(injection, XSS, 비밀정보 노출) 발견 시 즉시 수정

## 6. Git

- 새 브랜치에서 작업: `feature/*`, `fix/*`, `docs/*`
- Conventional Commits 형식 권장
- 커밋 전 변경 파일 간 정합성 확인
- 파괴적 명령(force push, reset --hard) 실행 전 반드시 확인 요청
- **커밋 메시지는 단일 `-m` 플래그** — heredoc 금지 (원격 세션 호환)

## 7. 커뮤니케이션

- **한국어** 응답 기본 (존댓말)
- 결론 먼저, 이유는 뒤에. 간결하게
- 작업 완료 후 불필요한 요약·반복 금지
- 판단이 필요한 선택지가 있으면 옵션을 제시하고 묻는다

## 8. 에이전트 팀

| 이름 | 역할 | 허용 도구 | 모델 |
|------|------|---------|------|
| 시타 (orchestrator) | 분석·계획·위임·종합 보고 | Read, Glob, Grep, Agent | claude-opus-4-7 |
| 시그마 (backend) | API·DB·서비스 구현 | Read, Write, Edit, Bash, Glob, Grep | claude-sonnet-4-6 |
| 픽셀 (frontend) | UI 구현 | Read, Write, Edit, Glob, Grep | claude-sonnet-4-6 |
| 모나미 (validator) | lint·test·보안 점검 | Read, Bash, Glob, Grep | claude-haiku-4-5-20251001 |

Agent MD 파일 위치: `~/.claude/agents/` (전역) 또는 `<project>/.claude/agents/` (프로젝트 전용)

## 9. 모나미 검증 의무화

- **모든 코드 수정·피처 변경 완료 후** 반드시 모나미(validator)에게 검증 요청
- 검증 범위: 코드 정합성, 보안, 테스트 통과율, 이전 실패 패턴 재발 여부
- 검수 FAIL 판정 시 수정 → 재검증 루프를 PASS될 때까지 반복
- 검증 없이 "완료" 선언 금지

## 10. 토큰 효율

- **출력 크기 제한**: Bash 출력 2KB 초과 예상 시 `| head -50` 또는 파일 리다이렉트 후 Read
- **에이전트 보고 요약**: 서브에이전트 결과는 PASS/FAIL + 핵심 3줄. 전문 복사 금지
- **재시도 제한**: 동일 명령 2회 실패 시 근본 원인 분석 전까지 재실행 금지
- **Windows 인코딩**: 한글 출력은 `PYTHONIOENCODING=utf-8` 또는 파일 리다이렉트 → Read (cp949 깨짐 방지)
- **배치 질문**: 관련 결정은 1개 메시지로 묶어서 질문
- **파일 읽기 중복 방지**: 같은 파일은 한 번에 필요한 범위를 통째로 Read
- **캐시 재활용**: 세션 내 동일 DB 쿼리 결과 재사용 (반복 실행 금지)
- **서브에이전트 위임**: 3회 이상 탐색 필요 또는 결과 >5KB 예상 시 Explore 에이전트 위임

## 11. 검토 원칙 — 객관성 의무

- **검토 요청 시 사용자 친화적 사고 배제** — 가능성 과장·리스크 축소 금지
- 기술적·구조적 문제는 해결 가능 여부와 무관하게 **있는 그대로 먼저 명시**
- "할 수 있다"고 말하기 전에 "왜 안 될 수 있는가"를 먼저 검토
- 치명적 리스크(UX 붕괴, 기술 한계, 비용 폭발)는 **굵게 강조**
- 칭찬과 격려는 검토 결과가 아니다 — 검토 문서에 포함 금지

## 12. 하네스 자동화 규칙

- **훅 자동화**: PostToolUse(Edit|Write) → 린터·보안 스캔 자동 실행 (지시서 §5.6)
  - 정책 강제 시 exit code **2** 사용 (exit 1은 무시됨)
  - 권장: Rust 기반 도구 (Ruff, Oxlint, Biome) — 50-100× 빠름
- **가드레일 4지점**: 사용자 입력 → 도구 호출 전 → 도구 응답 후 → 최종 출력 (지시서 §5.8)
- **플래너-생성-평가 분리**: 같은 에이전트가 계획+구현+검증 동시 수행 금지 (지시서 §5.10)
- **스파게티 방지**: 단일 파일 >300줄 / 함수 >50줄 / 순환 의존성 발견 즉시 구조 점검 (지시서 §11)

---

**END OF RULES — v2.1**
